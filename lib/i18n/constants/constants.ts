// eslint-disable-next-line no-underscore-dangle
declare let __webpack_hash__: string;

// Should be imported only inside static build, otherwise __webpack_hash__ will differ from the static
export const localesBuildHash = __webpack_hash__;
